# Initial values / solver warm-starts for `stage.yaml` (split-file example).
#
# IMPORTANT: this is *not* part of the calibration functor.
# Calibration is parameter-only (see spec_0.1c).
#
# These values are solver/algorithm inputs and may be handled by methodization
# or solver configuration in later phases.

initial_values:
  w: 1.0
  c: 0.9*w

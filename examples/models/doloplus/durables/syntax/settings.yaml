# Numerical / structural settings (consumed by configure functor)
settings:
  # Lifecycle
  T: 60

  # Financial asset grid (keeper exogenous grid dimension)
  n_a: 50
  a_min: 0.01
  a_max: 50

  # Housing asset grid
  n_h: 50
  h_min: 0.01
  h_max: 50

  # Total wealth grid (adjuster endogenous grid)
  n_w: 50
  w_min: 0.01
  w_max: 100

  # FUES upper envelope
  m_bar: 1.4

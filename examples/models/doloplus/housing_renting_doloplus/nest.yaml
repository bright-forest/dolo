name: HousingTenureLifecycle

periods:
  - housing_period: !period
  - housing_period: !period
  # ... (repeat for T periods)
  - terminal: !period

inter_connectors:
  - {b_o: a, H_nxt: H, y_o: y_pre, b_r: a, H_r: H, y_r: y_pre}
  - {b_o: a, H_nxt: H, y_o: y_pre, b_r: a, H_r: H, y_r: y_pre}
  # ... (one per period boundary)

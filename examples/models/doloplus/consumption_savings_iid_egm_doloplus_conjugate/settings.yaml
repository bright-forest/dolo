# Numerical/settings bindings for conjugate stage.yaml
#
# Identical to original - the semiconjugacy transformation preserves numerical settings.

settings:
  n_w: 100
  n_y_nodes: 9
  n_sim: 10000
  sim_seed: 42
  w_domain_min: 0.01
  w_domain_max: 4.0
  w_grid_min: 0.0
  w_grid_max: 4.0

# Numerical/settings bindings for the two-stage B3 sketch.

settings:
  # Wealth grid for the consumption stage
  n_w: 100
  w_domain_min: 0.01
  w_domain_max: 8.0
  w_grid_min: 0.0
  w_grid_max: 8.0

  # Quadrature nodes / sampling controls (placeholders for joint shocks)
  n_y_nodes: 9
  n_R_nodes: 9

